services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://api.sakeenapress.org}
    expose:
      - "80"
    restart: unless-stopped
